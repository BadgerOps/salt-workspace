# direnv configuration for automatic Nix shell activation
# See: https://direnv.net/
#
# Setup:
#   1. Install direnv: https://direnv.net/docs/installation.html
#   2. Hook into your shell (add to ~/.bashrc or ~/.zshrc):
#      eval "$(direnv hook bash)"  # or zsh
#   3. Allow this directory:
#      direnv allow
#
# The environment will automatically activate when you cd into this directory.

# Use flakes if available (recommended), otherwise fall back to shell.nix
if has nix; then
  if nix flake show &>/dev/null 2>&1; then
    use flake
  else
    use nix
  fi
fi
